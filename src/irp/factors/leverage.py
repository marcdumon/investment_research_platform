"""Leverage factors: debt_equity, net_debt_ebitda, interest_coverage. (phase 2)"""
import pandas as pd


def compute_leverage(
    income: pd.DataFrame,
    balance: pd.DataFrame,
) -> pd.DataFrame:
    raise NotImplementedError('leverage factors are phase 2')
